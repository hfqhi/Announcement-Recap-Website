Options -Indexes
RewriteEngine On

# 1. Map the root to public index

RewriteRule ^$ public/index.php [L]

# 2. Handle routing for internal pages

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.\*)$ public/$1 [L]
