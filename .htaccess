RewriteEngine On
RewriteBase /

# Force HTTPS

RewriteCond %{HTTPS} off
RewriteRule ^(.\*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Silently route root requests into the public folder

RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.\*)$ public/$1 [L]
